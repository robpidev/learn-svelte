const path = '/avanced/';

interface Path {
    url: string;
    name: string;
}

export const paths: Path[] = [
    { url: path + 'motion', name: 'Motion' },
    { url: path + 'transitions', name: 'Transitions' },
]
